#include "main.h"

/**
 * _abs - Entry point
 * @i: integer parameter to be checked
 * Description: computes the absolute value of an integer
 * Return: int
 */

int _abs(int i)
{

	if (i > 0)
	{
		return (i);
	}
	else if (i < 0)
	{
		return (i);
	}
	else
	{
		return (i);
	}
}
